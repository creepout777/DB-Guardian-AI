{
  "buildCommand": "if [ -n \"$SUPABASE_DB_URL\" ]; then npm run db:push; fi && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
